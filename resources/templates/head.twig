<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ function('wp_head') }}

    {% if options.get_theme_color %}
        {% include 'colors/change_colors.twig' %}
    {% endif %}

    {% include 'custom-css/custom-css.twig' %}
</head>