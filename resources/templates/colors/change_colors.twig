<style>
    article a,
    .fa.fa-star,
    #commentform a,
    .danzerpress-sidebar a {
        color: {{site.get_theme_color}};
    }
    ::-moz-selection { 
        background: {{site.get_theme_color}} !important; 
        color: white;
    }
    ::selection { 
        background: {{site.get_theme_color}} !important; 
        color: white;
    }
    .danzerpress-accent-background {
        background: {{site.get_theme_color}} !important;
    }
    a.danzerpress-button-modern,
    #submit,
    .danzerpress-sidebar input.search-submit {
        background: {{site.get_theme_color}};
        border-color: {{site.get_theme_color}};
    }
    header.drawer-navbar.drawer-navbar--fixed .drawer-container .drawer-menu li:hover {
        border-color: {{site.get_theme_color}};
    }
</style>